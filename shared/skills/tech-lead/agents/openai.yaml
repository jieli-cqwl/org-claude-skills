interface:
  display_name: "Tech Lead"
  short_description: "Create WBS-based implementation plans with critical path, dependencies, batches, and evidence"
  default_prompt: "Use $tech-lead to turn confirmed product, architecture, and test inputs into an AI-executable implementation plan."

policy:
  allow_implicit_invocation: false
