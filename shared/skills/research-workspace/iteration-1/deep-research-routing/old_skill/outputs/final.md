## Response

我理解你要对 **MCP（Model Context Protocol）** 做系统性 Deep Research，并用“横纵分析法”输出 **Markdown + PDF**。当前不能直接进入深度分析，因为 skill 硬约束要求：**必须先确认调研范围和关注维度**。

请确认以下范围，我再进入研究流程：

1. **横向分析范围**：默认覆盖 MCP 的生态位置、核心能力、协议机制、竞品/替代方案、典型实现、适用场景与风险边界。
2. **纵向分析范围**：默认深挖 MCP 架构、Client/Server/Host 关系、Tool/Resource/Prompt 机制、安全模型、集成成本、落地案例与失效条件。
3. **呈现目标**：默认 `understanding`，目标是建立清晰认知，而不是立即选型决策。
4. **输出产物**：默认先生成 `docs/mcp/research-report.md`，用户确认后再导出 PDF。

如果这个默认范围正确，请回复“确认”。如果你更关心“是否采用 MCP / 如何落地 / 和 Function Calling、插件、Agent SDK 对比”，请直接改范围。

## Self Check
- route: `/research`
- mode: `analysis`
- presentation_profile: `understanding`
- artifacts_required: `docs/mcp/research-report.md` + PDF（需范围确认后进入研究，报告完成并确认后再导出）