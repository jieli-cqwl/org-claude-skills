interface:
  display_name: "Commit"
  short_description: "Git safe commit and push workflow"
  default_prompt: "Use $commit to safely commit and push changes."

policy:
  allow_implicit_invocation: false
