interface:
  display_name: "Scan"
  short_description: "Project-wide code health, tech debt, and performance audit"
  default_prompt: "Use $scan to audit project code health, tech debt, and performance risks."

policy:
  allow_nested_agents: true
  codex_execution: inline
  execution_kind: orchestrator
  allow_implicit_invocation: false
