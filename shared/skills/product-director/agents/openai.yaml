interface:
  display_name: "Product Director"
  short_description: "Strategic product scoping and director baseline confirmation"
  default_prompt: "Use $product-director to clarify the root problem, scope phases, and confirm the director baseline."

policy:
  allow_implicit_invocation: false
