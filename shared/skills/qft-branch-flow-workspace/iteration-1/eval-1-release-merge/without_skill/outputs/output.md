收到。目标我先对齐如下：处理 `qft-all`、`qft-app`、`qft-system` 三个仓库，版本号 `0301`，版本分支为 `V.0301`；合并方向是“需求分支 -> 版本分支”。如果版本分支不存在，我会基于对应主分支创建：`qft-all` 从 `3.0.0.MASTER` 创建，`qft-app` 和 `qft-system` 从 `master` 创建。

执行前还缺一个必须由研发确认的信息：每个仓库要合入的需求分支。请按下面格式提供，某个仓库不处理就写“跳过”：

```text
qft-all: <需求分支名>
qft-app: <需求分支名>
qft-system: <需求分支名>
```

拿到分支名后，我会逐仓库检查工作区状态、远端分支、版本分支是否存在，然后按“需求分支合入 `V.0301`”处理；如有冲突或前置条件不满足，会停下来把具体仓库、冲突文件或缺失条件报给你裁决。

合并完成后我会先汇总待推送内容。推送需要你单独确认，我不会默认推送；只有你明确回复“确认推送”后，我才会推送对应仓库的 `V.0301` 分支。