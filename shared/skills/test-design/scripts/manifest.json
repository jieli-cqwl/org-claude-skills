{
  "schema_version": "1.0.0",
  "scripts": [
    {
      "id": "completion-check",
      "path": "scripts/completion_check.sh",
      "owner": "test-design",
      "allowed_args": ["hook payload via stdin only", "--help", "-h"],
      "denied_args": ["--exec", "--shell", "--network", ";", "&&", "|"],
      "external_commands": ["bash", "python3", "dirname", "jq", "mktemp", "rm", "sed"],
      "timeout_seconds": 15,
      "output_limit_bytes": 65536,
      "output_root": ".",
      "allowed_output_roots": ["$TMPDIR", "/tmp"],
      "allowed_input_roots": [
        "docs",
        "contracts/canonical",
        "shared/runtime",
        "tools/community"
      ],
      "failure_state": "test-design canonical gate blocks handoff until test-cases.json has AC triple coverage, QA handoff source refs, design dependency closure, and review convergence evidence",
      "failure_message": "test-design completion gate failed",
      "exit_code_meanings": {
        "0": "hook decision emitted or help displayed; decision may be allow or block",
        "1": "script initialization or validation failed before hook decision",
        "2": "fail closed block decision emitted"
      },
      "shell_parameter_strategy": "argv only; hook payload via stdin only; no shell interpolation",
      "verification_command": "bash tests/test-test-design-completion-gate.sh && bash tests/test-design-skill-governance-redesign.sh && bash tests/test-skill-output-and-gate-contract.sh"
    }
  ]
}
