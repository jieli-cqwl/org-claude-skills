interface:
  display_name: "Review"
  short_description: "Deep code review with actionable suggestions"
  default_prompt: "Use $review to perform a deep code review of changes."

policy:
  allow_nested_agents: false
  agent_type: code-reviewer
  execution_kind: agent_backed
  allow_implicit_invocation: false
