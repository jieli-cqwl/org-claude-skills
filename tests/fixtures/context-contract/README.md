# Context Contract Fixtures

The executable context-contract tests build their positive and negative fixture repositories in temporary directories so each scenario can mutate isolated copies. This directory reserves the stable fixture root named in the Phase 1 design for future golden recovery snapshots.
