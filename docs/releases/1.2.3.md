# org-claude-skills v1.2.3 发布说明

## 核心变更

1. 收口 `/project-manager` Phase 3 强门禁矩阵，新增唯一可执行规则源 `shared/skills/project-manager/scripts/phase3-grade-matrix.sh`。
2. 修复 `project-manager` `completion_check.sh` 的 bash 3.2 兼容性问题，避免 macOS 默认 `/bin/bash` 下失效。
3. 收口 `codex-doc-review` 的 canonical 输出目录解析，补齐 misplaced report 自动修复脚本。
4. 修复 `repair_misplaced_reports.py` 未遵守 `ORG_STATE_ROOT` 的归档路径问题。
5. 修复 `codex-doc-review` 对模板格式 `审查阶段 (stage)` 的解析兼容性。
6. 新增回归测试：
   - `tests/test-project-manager-phase3-contract.sh`
   - `tests/test-codex-doc-review-routing.sh`
   - `tests/test-codex-doc-review-repair.sh`
   - `tests/test-install-runtime-audit.sh`
7. 更新运行验收与发布清单，明确额外系统 skills 允许存在，但不得缺失或遮蔽仓库托管技能。

## 升级命令

```bash
cd ~/org-claude-skills
git pull --rebase
bash install.sh --target all --force --merge-hooks --check full
bash tools/dev/probe-runtime-capabilities.sh ~/org-claude-skills
```

## 验收重点

- `bash tests/run-all.sh` 通过
- `bash tools/dev/probe-runtime-capabilities.sh ~/org-claude-skills` 无 `[FAIL]`
- `bash tests/test-project-manager-phase3-contract.sh` 通过
- `bash tests/test-codex-doc-review-routing.sh` 通过
- `bash tests/test-codex-doc-review-repair.sh` 通过
- `bash tests/test-install-runtime-audit.sh` 通过

## 已知边界

- Claude：
  - 可依赖全局 hooks 与 skill-local Stop hook
- Codex：
  - 可依赖 skills 与 agent 委派
  - 不把 hooks 当成强保障
  - 对 `completion_check.sh` 以显式脚本执行为准
  - 当前仍保留已知 WARN：
    - `Codex skill-local Stop hook 未触发`
    - `Codex hooks.json 默认未捕获任何事件`
