# org-claude-skills v1.2.4 发布说明

## 核心变更

1. 新增 `superpowers + OpenSpec` 最佳实践设计草稿：`openspec/designs/2026-03-28-superpowers-openspec-best-practice-draft.md`。
2. 系统性修正 selected `community/superpowers` 中文 runtime，清除 `与。执行计划`、`超级大国：完成开发分支` 一类误译，并恢复 `skill id`、命令、路径、代码、术语缩写的英文保真。
3. 强化 `tools/community/sync_canonical_from_upstream.py` 的本地化保护边界，补齐来源头再生、`vs.` / `superpowers:*` / 术语缩写保护，以及嵌套 fenced code block 保护。
4. 新增 `tests/test-community-tools.sh` 回归测试，阻断来源头丢失、skill 标识符被自然语言化、代码块被翻译污染等已知回归。
5. 更新 `README.md` 与 `shared/reference/Skill质量标准.md`，统一 `community/superpowers` 中文 runtime 的治理口径。

## 升级命令

```bash
cd ~/org-claude-skills
git pull --rebase
bash install.sh --target all --force --merge-hooks --check full
bash tools/dev/probe-runtime-capabilities.sh ~/org-claude-skills
```

## 验收重点

- `bash tests/run-all.sh` 通过
- `bash install.sh --target all --dry-run --force` 输出安装计划
- `bash install.sh --target all --force --merge-hooks --check full` 通过
- `bash tools/dev/probe-runtime-capabilities.sh ~/org-claude-skills` 无 `[FAIL]`
- `codex exec --json "List all currently available skills by exact name only, one per line, no extra text."` 结果符合仓库当前 skill 可见性口径

## 已知边界

- Claude：
  - 可依赖全局 hooks 与 skill-local Stop hook
- Codex：
  - 可依赖 skills 与 agent 委派
  - 不把 hooks 当成强保障
  - 对 `completion_check.sh` 以显式脚本执行为准
  - 当前仍保留已知 WARN：
    - `Codex skill-local Stop hook 未触发`
    - `Codex hooks.json 默认未捕获任何事件`
