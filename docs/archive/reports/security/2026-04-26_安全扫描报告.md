# 安全扫描报告 — org-claude-skills

> 扫描日期：2026-04-26

## 安全评分

88 / 100

评分依据：secret scanning 当前工作区与 Git 历史均已清零；Bandit HIGH/MEDIUM 已清零；安装 dry-run、quick 回归与 runtime probe 均通过。剩余扣分来自 131 个 Bandit LOW 历史低风险项，以及本机曾出现 ignored eval 日志 token 形态，凭据侧仍需按泄露口径完成轮换。

## Fresh 工具证据

- `gitleaks detect --source . --redact --verbose --report-format json --report-path docs/reports/security/2026-04-26_gitleaks-current-worktree.json --no-git`：PASS，`no leaks found`
- `gitleaks detect --source . --redact --verbose --report-format json --report-path docs/reports/security/2026-04-26_gitleaks-history.json`：PASS，389 commits scanned，`no leaks found`
- `bandit -r tools shared tests examples -x tests/fixtures,tools/eval/results -f json -o /tmp/bandit-after.json`：HIGH=0，MEDIUM=0，LOW=131；JSON 已同步到 `docs/reports/security/2026-04-26_bandit.json`
- `bash tests/run-all.sh --quick --profile`：PASS，83/83，`All tests passed`
- `bash tools/dev/probe-runtime-capabilities.sh /Users/lijieli/org-claude-skills`：PASS，Claude/Codex 运行时能力探针全绿
- `bash install.sh --target all --dry-run --check full`：PASS，Claude 1226 files，Codex 1243 files
- `git submodule status`：PASS，无 malformed submodule 报错
- `git diff --check`：PASS

## 当前分级

| 等级 | 数量 | 状态 |
|------|------|------|
| 严重 | 0 | 无阻塞 |
| 高危 | 0 | 已清零 |
| 中危 | 0 | 已清零 |
| 低危 | 131 | 保留为后续技术债治理，不阻塞团队安装 |

## 已处理风险

### VULN-001: 测试 fixture 触发 secret scanner

- 初始影响：`tests/test-feishu-docs-wrapper.py` 中 redaction fixture 触发当前工作区、Git 历史与发布快照 secret scanning。
- 处置：测试 fixture 改为运行时拼接，避免静态 secret 形态；历史 fixture 通过 `.gitleaks.toml` 限定到 redaction fixture 文件处理。
- 验证：当前工作区 gitleaks PASS；Git history gitleaks PASS；`python3 tests/test-feishu-docs-wrapper.py` PASS。

### VULN-002: 本机 ignored eval 日志出现 token 形态

- 初始影响：本地 ignored `.log` 文件中存在凭据形态输出；未进入 Git 发布快照，但按安全口径视为已暴露。
- 处置：已删除本机日志；`.gitignore` 增加 `tools/eval/results/**/*.log`，降低团队误纳入风险。
- 待用户完成：在凭据提供方轮换对应 Anthropic 凭据；我无法替你完成外部凭据轮换。
- 验证：当前工作区 gitleaks PASS。

### VULN-003: 外部 XML 解析 hardening

- 初始影响：`shared/skills/deep-research/scripts/arxiv_search.py` 使用标准库 XML parser 解析 arXiv 响应，Bandit 报 MEDIUM。
- 处置：改用 `defusedxml.ElementTree`；补充 arXiv HTTPS host/path 校验。
- 验证：`python3 shared/skills/deep-research/scripts/arxiv_search.py --query 'machine learning' --max-results 1 --timeout 20` PASS；Bandit MEDIUM=0。

### VULN-004: malformed gitlink/submodule

- 初始影响：`claude-code-engineering` 是无 `.gitmodules` 映射的 gitlink，`git submodule status` 失败，并放大本地 secret-scan 噪音。
- 处置：从 Git index 移除该 gitlink；保留本地目录但加入 `.gitignore` 与 gitleaks allowlist。
- 验证：`git submodule status` PASS；gitleaks PASS。

### VULN-005: runtime probe 兼容性问题

- 初始影响：Claude auth、global hooks、CCR runtime link activation 探针出现 FAIL 片段。
- 处置：auth 探针改用原生 `claude auth status`；临时 HOME 复制 CCR 运行时依赖；global hook 探针合并 CCR settings；嵌套 reference 读取去掉外层 exactly-once 冲突。
- 验证：`bash tools/dev/probe-runtime-capabilities.sh /Users/lijieli/org-claude-skills` PASS。

## 结论

安全和运行时门禁已达到团队安装放行标准。唯一不能由仓库内修复完成的事项是凭据侧轮换：请把本机 ignored eval 日志中曾出现过的 Anthropic 凭据按泄露处理完成轮换。完成轮换后，可以正式进入团队安装 rollout。
