# 团队安装使用把关报告

> 日期：2026-04-26

## 结论

`GO_WITH_CREDENTIAL_ROTATION`

仓库侧、安装侧、运行时侧、安全扫描侧已经可以放行团队安装使用；唯一外部前置项是把本机 ignored eval 日志中曾出现过的 Anthropic 凭据按泄露口径完成轮换。轮换完成后，可以正式写入团队安装说明并推进 rollout。

## 验证面

| 维度 | 状态 | Fresh 证据 |
|------|------|------------|
| 安装入口 | 已通过 | `bash install.sh --target all --dry-run --check full`：PASS，Claude 1226 files，Codex 1243 files |
| 合同校验 | 已通过 | dry-run 内置 contract check 输出 `[PASS] context contract` 与 `OK: all checks passed` |
| quick 回归 | 已通过 | `bash tests/run-all.sh --quick --profile`：83/83，`All tests passed` |
| full 回归基线 | 已通过 | 本轮修复前已跑 `bash tests/run-all.sh --profile`：87/87，`All tests passed`；修复后补跑 quick + 定向探针 |
| 上下文管理 | 已通过 | active-doc scope、context validator、context recovery、context audit 均在回归中 PASS |
| 运行时探针 | 已通过 | `bash tools/dev/probe-runtime-capabilities.sh /Users/lijieli/org-claude-skills`：Claude/Codex auth、hooks、skill parse、entry/rule runtime contract、agent delegate 均 PASS |
| 安全扫描 | 已通过 | Gitleaks 当前工作区 PASS；Gitleaks Git history PASS；Bandit HIGH=0/MEDIUM=0/LOW=131 |
| 仓库结构 | 已通过 | `git submodule status`：PASS，无 malformed gitlink 报错 |
| 格式卫生 | 已通过 | `git diff --check`：PASS |

## 已修复阻塞项

- secret-scanner fixture：`tests/test-feishu-docs-wrapper.py` 改为运行时拼接，保留 redaction 行为验证。
- 本机 ignored token 日志：已删除日志，并用 `.gitignore` 阻止 eval `.log` 误纳入。
- malformed gitlink：已从 Git index 移除 `claude-code-engineering`，本地教学目录继续忽略。
- arXiv XML/parser：改用 `defusedxml`，并约束 arXiv HTTPS endpoint。
- GitHub release lookup：约束 GitHub repo URL 与 API host，消除 Bandit MEDIUM。
- runtime probe：修复 Claude auth、CCR 临时 HOME、global hooks settings 合并和嵌套 reference 探针稳定性。

## 剩余风险

- 凭据轮换：我已清理本机日志和扫描面，但外部 Anthropic 凭据是否轮换只能由你在凭据提供方确认。
- Bandit LOW：当前 131 个 LOW 不阻塞安装，但建议后续单独排期做安全债治理。
- 工作区已有其他未提交改动：本次修复没有回滚用户已有变更，正式发布前需要按团队流程一起 review/stage/commit。

## Rollout 建议

1. 先完成 Anthropic 凭据轮换。
2. 提交本次修复与报告。
3. 团队安装命令使用：`bash install.sh --target all --check full`。
4. 首批团队成员安装后，抽样运行：`bash tools/dev/probe-runtime-capabilities.sh /path/to/org-claude-skills`。

## Fresh Proving Commands

```bash
bash install.sh --target all --dry-run --check full
bash tests/run-all.sh --quick --profile
bash tools/dev/probe-runtime-capabilities.sh /Users/lijieli/org-claude-skills
gitleaks detect --source . --redact --verbose --no-git
gitleaks detect --source . --redact --verbose
bandit -r tools shared tests examples -x tests/fixtures,tools/eval/results -f json
git submodule status
git diff --check
```
