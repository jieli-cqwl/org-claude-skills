# skill-optimizer runtime source archive

ARCHIVE_ONLY.

This directory preserves the former `skill-optimizer` runtime source for historical review. It is not an active Skill discovery source, install source, adapter source, or runtime reference path. Active Skill quality review and optimization governance now route through `skill-harness` and `shared/reference/Skill质量标准.md`.
