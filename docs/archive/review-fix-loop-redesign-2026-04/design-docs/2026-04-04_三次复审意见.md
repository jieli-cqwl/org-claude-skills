# review-fix-loop 重设计三次复审意见

## 复审对象

- `claude/skills/code-review-fix/SKILL.md`
- `claude/skills/doc-review-fix/SKILL.md`
- `tests/test-review-fix-redesign-contract.sh`
- `tests/test-review-fix-redesign-scenarios.sh`
- `tests/test-single-source-layout.sh`
- `docs/review-fix-loop/2026-04-03-redesign/fix-1.md`

## 复审目标

检查本轮修复后，是否已经同时满足以下要求：

1. 两个新 skill 的运行时契约已经承接 `design.md`
2. redesign contract test 已能拦住 skeleton/缺契约场景
3. layout gate 不再容忍旧 skill/agent 回流
4. 修复过程具备 RED/GREEN 和 full-suite 证据

## Findings

本轮未发现新的阻塞项。

## 已排除问题

- 已排除 “contract 仍然过浅”：
  - `tests/test-review-fix-redesign-contract.sh` 已对 `codex exec --json`、schema、baseline/stash、收敛条件、最终报告字段和 DECEPTION 模式数量建立断言。
- 已排除 “缺少执行层机械验证”：
  - `tests/test-review-fix-redesign-scenarios.sh` 已补上 11 个状态机场景，覆盖 dirty tree、clean tree、stash conflict、code/doc 正向循环、non-json、缺字段、max-round、不收敛、user abort 和 fail-close。
- 已排除 “旧对象仍可回流到活跃源码树”：
  - `tests/test-single-source-layout.sh` 现在只允许 `code-review-fix`、`doc-review-fix` 两个 Claude-only skill，且 `claude/agents` 不再放行任何 `.md` agent。
- 已排除 “本轮修复破坏仓库总门禁”：
  - `bash tests/run-all.sh`（24 项）通过，`python3 tools/community/check_task_plan_consistency.py ...` 通过。

## 结论

结论：`APPROVE`

当前实现已经从“迁移完成但契约偏空”提升到“迁移、契约、门禁、场景验证和全量回归证据一致”。我认为这轮可以收口。

## 剩余风险

- 当前信心已经包含契约门禁与状态机场景验证，但仍没有额外做一次真实交互式 LLM 回路演练；如果后续要进一步提高运行时信心，建议补一条人工演练记录，但这不是本轮阻塞项。
