# 2026-04-02 未登录访问首页跳转登录页 单样本记录

记录日期：`2026-04-02`  
记录人：`Codex`  
样本标题：`未登录访问首页跳转登录页`

## 基本信息

- 需求类型：访问保护与路由收口
- 风险等级：中
- 是否符合小需求边界：是
- 若不符合，为什么仍进入试点：不适用

## 执行前检查

- `python3 tools/community/source_lock_check.py`：沿用本周启动结论
- `bash tests/test-superpowers-boundary.sh`：沿用本周启动结论
- `bash tests/test-community-first-boundary.sh`：沿用本周启动结论
- `bash tests/run-all.sh`：通过
- `bash install.sh --target all --force --merge-hooks --check full`：沿用本周启动结论
- `bash tools/dev/probe-runtime-capabilities.sh ~/org-claude-skills`：当前运行面无阻断性 `FAIL`

## 执行路径

1. 需求输入：用户未登录时访问首页，应先跳回登录页
2. 是否默认进入 `brainstorming`：是，本轮先判断是否会扩成完整鉴权策略
3. 是否顺利进入 `writing-plans`：是，可收口为路由守卫最小闭环
4. 是否顺利进入执行链：本轮停在模拟样本层，不进入真实业务实现
5. 是否需要退回标准链：否

## 工件与证据

- 设计草稿路径：沿用登录样本的边界判断方法，不单独新增设计稿
- 计划路径：不单独新增计划稿，作为首周样本池中的第二条轻量样本
- review 结论：可作为 route-guard 类小需求样本
- verification 结论：模拟样本层结论为通过
- 关键命令与输出摘要：
  - `bash tests/test-doc-reference-integrity.sh` -> `PASS`
  - `bash tests/run-all.sh` -> `All tests passed`

## 问题记录

- 卡点：该需求容易自然扩展成“回跳原页面”或“权限矩阵”，需要主动收边界
- 临时规避：本轮只收口为“未登录访问首页 -> 跳登录页”
- 是否属于文档口径问题：否
- 是否属于 runtime/安装问题：否

## 结论

- 最终状态：`PASS`
- 总耗时：`一次样本级模拟演练`
- 是否建议继续把这类需求纳入试点：建议，但必须限制在最小 route guard 闭环
- 后续动作：
  - 在真实业务仓库中验证首页守卫是否会自然牵出回跳策略
  - 若牵出回跳参数，则应视情况转标准链
