# 2026-03-31 试点预演记录

记录日期：`2026-03-31`  
记录人：`Codex`  
样本标题：`community-first 仓库级试点预演`

## 基本信息

- 需求类型：仓库级试点前最小检查预演
- 风险等级：中
- 是否符合小需求边界：不适用
- 若不符合，为什么仍进入试点：本次不是业务需求实现，而是试点入口、前置检查和运行面口径的仓库级预演

## 执行前检查

- `python3 tools/community/source_lock_check.py`：`PASS`
- `bash tests/test-superpowers-boundary.sh`：`PASS`
- `bash tests/test-community-first-boundary.sh`：`PASS`
- `bash tests/run-all.sh`：`PASS`，结果为 `All tests passed`
- `bash install.sh --target all --force --merge-hooks --check full`：`PASS`
- `bash tools/dev/probe-runtime-capabilities.sh ~/org-claude-skills`：`FAIL`

## 执行路径

1. 需求输入：补齐试点执行包并做一次真实预演
2. 是否默认进入 `brainstorming`：是，本轮先完成方案收口再进入执行
3. 是否顺利进入 `writing-plans`：是，本轮按多步骤执行思路拆解并落地
4. 是否顺利进入执行链：是，文档、合同、模板和记录目录均已落地
5. 是否需要退回标准链：否

## 工件与证据

- 执行入口：`docs/community-first/试点执行入口.md`
- 模板目录：`docs/community-first/templates/`
- 关键命令与结果：
  - `python3 tools/community/source_lock_check.py` -> `[PASS] source lock valid`
  - `bash tests/test-superpowers-boundary.sh` -> `[PASS] superpowers boundary`
  - `bash tests/test-community-first-boundary.sh` -> `[PASS] community-first boundary`
  - `bash tests/run-all.sh` -> `All tests passed`
  - `bash install.sh --target all --force --merge-hooks --check full` -> `安装流程完成：target=all, version=1.2.4-b61c126-dirty`
- runtime probe 失败摘要：
  - Claude bare 最小调用失败
  - Claude 常规模式最小调用失败
  - Claude 全局 hooks 探针失败
  - Claude skill-local hook 探针失败
  - Claude agent 委派探针失败
  - 失败原因为代理侧返回 `403`，提示 `用户额度不足`
  - `timeout 20 codex exec --json 'Reply with exactly OK.'` 退出码为 `124`
  - `pgrep -af 'codex exec'` 仍可见长期残留进程：`11258`、`14547`

## 问题记录

- 卡点：
  - 当前机器的 Claude 运行面不可用，原因不是仓库资产缺失，而是代理额度或鉴权失败
  - 当前机器的 Codex 运行面最小探针超时，且存在长期残留 `codex exec` 进程
- 临时规避：
  - 仓库侧继续保留试点执行包、边界检查和安装检查
  - 运行面试点需在修复代理额度和 Codex 进程阻塞后再继续
- 是否属于文档口径问题：部分是，已将执行入口、模板和历史说明补齐
- 是否属于 runtime/安装问题：是，主要集中在 Claude 代理额度与 Codex 运行面超时

## 结论

- 最终状态：`FAIL`
- 总耗时：单轮仓库级预演约一小时内完成
- 是否建议继续把这类需求纳入试点：建议继续保留仓库级预演，但当前机器不建议直接作为团队试点样板机
- 后续动作：
  - 修复 Claude 代理额度或鉴权
  - 清理或定位长期残留的 `codex exec` 进程
  - 运行面恢复后，重新执行 `bash tools/dev/probe-runtime-capabilities.sh ~/org-claude-skills`
