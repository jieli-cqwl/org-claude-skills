# Community-First 单样本记录模板

记录日期：`YYYY-MM-DD`  
记录人：`<name>`  
样本标题：`<sample-title>`

## 基本信息

- 需求类型：
- 风险等级：
- 是否符合小需求边界：
- 若不符合，为什么仍进入试点：

## 执行前检查

- `python3 tools/community/source_lock_check.py`：
- `bash tests/test-superpowers-boundary.sh`：
- `bash tests/test-community-first-boundary.sh`：
- `bash tests/run-all.sh`：
- `bash install.sh --target all --force --merge-hooks --check full`：
- `bash tools/dev/probe-runtime-capabilities.sh ~/org-claude-skills`：

## 执行路径

1. 需求输入：
2. 是否默认进入 `brainstorming`：
3. 是否顺利进入 `writing-plans`：
4. 是否顺利进入执行链：
5. 是否需要退回标准链：

## 工件与证据

- 设计草稿路径：
- 计划路径：
- review 结论：
- verification 结论：
- 关键命令与输出摘要：

## 问题记录

- 卡点：
- 临时规避：
- 是否属于文档口径问题：
- 是否属于 runtime/安装问题：

## 结论

- 最终状态：`PASS / FAIL / 转标准链`
- 总耗时：
- 是否建议继续把这类需求纳入试点：
- 后续动作：
