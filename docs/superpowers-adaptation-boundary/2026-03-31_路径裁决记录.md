# 2026-03-31 路径裁决记录

适用范围：
- `community/superpowers`
- `openspec/`
- `docs/community-first`

目的：
- 为 T1 的路径收口提供当前裁决
- 为后续 `docs/community-first/boundary-contract.md` 与 `contracts/superpowers-boundary.yaml` 提供输入

## 当前路径裁决

1. 设计草稿路径以 `openspec/designs/` 为准
2. 实施计划路径以 `openspec/plans/` 为准
3. `docs/superpowers/specs/` 与 `docs/superpowers/plans/` 不再作为现行路径
4. 若仓库仍出现 `docs/superpowers/...`，默认按历史残留处理：
   - T1 先清理显性示例与正文损坏
   - T3 再系统性迁移 prompt 模板、示例和深分叉残留
   - T4/T5 通过 machine-readable contract 决定是否仍允许极少数兼容残留

## 当前已确认的 T1 处理对象

- `community/superpowers/skills/requesting-code-review/SKILL.md`
  - 已修复异常噪音文本
  - 已把计划示例路径改为 `openspec/plans/...`
- `community/superpowers/skills/brainstorming/spec-document-reviewer-prompt.md`
  - 已把 spec 文档示例路径改为 `openspec/designs/`
- `community/superpowers/skills/subagent-driven-development/SKILL.md`
  - 已把计划文件示例路径改为 `openspec/plans/...`

## 本期不交付

- 不在 T1 完成所有 `opsx:*` 链路改写
- 不在 T1 决定 `community/openspec` 的最终归档方式
- 不在 T1 一次性清理全部文档层历史表述

## 后续依赖

- T2：把本裁决并入 `docs/community-first/boundary-contract.md`
- T4：把本裁决沉到 `contracts/superpowers-boundary.yaml`
- T5：基于合同为历史路径建立自动检测
