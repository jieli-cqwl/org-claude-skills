# 2026-04-01 发布强门禁交付验收记录

验收时间：2026-04-01 14:33:00 +0800  
验收对象：`org-claude-skills` 第一阶段“发布前强门禁做实”  
证据基线：`git rev-parse --short HEAD = c3acca9`（`origin/main` 已同步；当前工作区含本地未跟踪目录 `?? .claude/`）

## 验收范围

- 决策口径：验证发布强门禁最小闭环是否已落到 `main` 并完成本地强验证留证
- 本期不交付：新版本 tag 发布、发布公告、发布后人工抽样、同事机器复验
- 接受边界：`Codex skill-local Stop hook 未触发` 作为已知 `WARN` 显式留证，不做静默忽略

## 本轮交付

1. `release.yml` 改为 `resolve-context -> validate-release -> publish`，发布前先校验再发布。
2. 新增 `tools/release/validate-release-metadata.sh`，收口 tag / `VERSION` / `CHANGELOG.md` / `docs/releases` 一致性规则。
3. 新增 `tests/test-release-metadata.sh`，并接入 `tests/run-all.sh` 主门禁。
4. 修复本地测试环境 `rg` 可执行性问题，消除 `constraint closure contract test` 的环境假失败。
5. 更新 `docs/release-checklist.md` 与 `docs/runtime-acceptance-sop.md`，明确 CI 阻断、本地强验证、人工抽样三层口径。
6. 变更已以 `c3acca9 fix: gate releases before publish` 合并并推送到 `origin/main`。

## 核心证据

### 1. 提交与主分支同步

执行：

```bash
git -C ~/org-claude-skills log --oneline --decorate -2
git -C ~/org-claude-skills push origin main
git -C ~/org-claude-skills rev-list --left-right --count origin/main...main
```

结果：

- 本地 `main` 已 fast-forward 到 `c3acca9`
- `git push origin main` 成功
- `origin/main` 与本地 `main` 同步，ahead/behind = `0/0`

### 2. 仓库自动化门禁

执行：

```bash
cd ~/org-claude-skills
bash tests/run-all.sh
```

结果：

- `All tests passed`
- 新增 `release metadata test` 共 5 个场景全部通过
- `constraint closure contract test` 通过
- 现有主门禁 `test / validate` 所依赖的自动化总入口保持可用

### 3. 发布前静态门禁

执行：

```bash
cd ~/org-claude-skills
bash install.sh --target all --dry-run --force
bash tools/release/validate-release-metadata.sh v1.2.4 ~/org-claude-skills
```

结果：

- dry-run 输出计划写入：Claude `249` 个文件，Codex `245` 个文件
- 发布元数据校验通过：`[release-metadata][PASS] v1.2.4 对应的版本元数据一致`

### 4. 本地强验证

执行：

```bash
cd ~/org-claude-skills
bash install.sh --target all --force --merge-hooks --check full
bash tools/dev/probe-runtime-capabilities.sh ~/org-claude-skills
codex exec --json "List all currently available skills by exact name only, one per line, no extra text."
```

结果：

- `install.sh --check full` 通过
- `~/.org-skills-state/claude/pruned-manifest` 与 `~/.org-skills-state/codex/pruned-manifest` 存在
- `~/.claude` 与 `~/.codex` 根目录未发现 `.org-*` 与 `.org-backups/`
- Claude 运行面：`Auth`、`Proxy`、`Minimal Bare`、`Regular Output`、`Global Hooks`、`Skill Local Hook`、`Agent Delegate` 全部 `PASS`
- Codex 运行面：`Minimal Exec`、`Skills`、`OpenSpec Propose`、`Global Hooks`、`Agent Delegate` 为 `PASS`
- Codex 技能可见性包含 `brainstorming`，自动暴露面符合当前仓库预期

## 已知 WARN 与边界

1. `Codex skill-local Stop hook 未触发`
   - `probe-runtime-capabilities.sh` 退出码为 `0`
   - 当前无新增 `FAIL`
   - 该项必须保留在留证中，不可省略

2. 当前 `main` 工作区含本地未跟踪目录 `?? .claude/`
   - 这是本地运行目录，不属于本次提交内容
   - 若后续执行版本 tag 发布前验收，必须在 clean branch / clean context 下重新按 `docs/release-checklist.md` 确认

3. 发布后人工抽样未在本记录范围内执行
   - `docs/release-checklist.md` A3 仍要求在真正版本发布后补齐干净 HOME、同事机器与完整运行验收抽样

## 结论

- 第一阶段“发布前强门禁做实”交付验收：`PASS`
- 版本 tag 发布验收完成状态：`WARN`

原因：

- 当前变更的代码、测试、文档、本地安装与运行时证据已经闭环
- 已知 `WARN` 已如实留证，没有被模糊表述覆盖
- 本记录不替代后续真正版本发布时的 A1/A2/A3 完整验收
