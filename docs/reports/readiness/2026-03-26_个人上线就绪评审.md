# 2026-03-26 个人上线就绪评审

评审时间：2026-03-26 17:04:22 +0800  
评审对象：`org-claude-skills`  
证据基线：`git rev-parse --short HEAD = b68e90e`（当前工作区为 dirty）

## 评审范围

- 决策口径：个人正式使用
- 非本次口径：团队全面推广 / 发布前 clean branch 验收
- 接受边界：Codex hooks 不作为强保障，改以显式 `completion_check.sh` 和运行文档补位

## 本轮已修复

1. 收口 `/project-manager` Phase 3 规则漂移  
   - 统一强门禁矩阵：
     - 轻量：`REVIEW_A + QA_A`
     - 标准：`REVIEW_A + REVIEW_B + QA_A + QA_C`
     - 完整：`REVIEW_A + REVIEW_B + QA_A + QA_B + QA_C + QA_D`
   - `REVIEW_C` 明确降级为可选增强审查，不再冒充强门禁
2. 新增机械回归  
   - `tests/test-project-manager-phase3-contract.sh`
   - 覆盖 `SKILL.md / phase3-dispatch / templates / completion_check / 验收文档` 的一致性
3. 收口运行口径  
   - Codex skill 可见性判定改为：仓库托管 skills 不缺失即可；额外系统 skills 允许存在但不能重名冲突
4. 收口 `codex-doc-review` 修复与解析链路
   - `repair_misplaced_reports.py` 改为遵守 `ORG_STATE_ROOT`，归档路径不再硬编码到默认 HOME
   - hook / repair 脚本的 `scope` 解析兼容模板元信息 `审查阶段 (stage): ...`
   - `tests/test-codex-doc-review-repair.sh` 与 `tests/test-codex-doc-review-routing.sh` 改为按真实模板格式回归
5. 恢复运行文档契约
   - `docs/runtime-acceptance-sop.md` 与 `docs/release-checklist.md` 重新明确：允许额外系统 skills，但仓库托管 skills 不得缺失且不能重名冲突

## 核心证据

### 1. 自动化回归

执行：

```bash
cd ~/org-claude-skills
bash tests/run-all.sh
```

结果：

- `All tests passed`
- 新增 `project-manager phase3 contract test` 通过
- 新增 `codex-doc-review repair test` 通过
- 新增 `codex-doc-review routing test` 通过

### 2. 真实运行探针

执行：

```bash
cd ~/org-claude-skills
bash tools/dev/probe-runtime-capabilities.sh ~/org-claude-skills
```

结果：

- Claude：
  - `Auth`: PASS
  - `Proxy`: PASS（当前代理：`http://127.0.0.1:4000`）
  - `Minimal Bare`: PASS
  - `Regular Output`: PASS
  - `Global Hooks`: PASS
  - `Skill Local Hook`: PASS
  - `Agent Delegate`: PASS
- Codex：
  - `Minimal Exec`: PASS
  - `Skills`: PASS
  - `Skill Local Hook`: WARN
  - `Global Hooks`: WARN
  - `Agent Delegate`: PASS

复验结论：在追加修复 `codex-doc-review` 与运行文档契约后再次执行，结果保持不变，无新增 `FAIL`。

### 3. Codex 技能可见性

执行：

```bash
cd ~/org-claude-skills
codex exec --json "List all currently available skills by exact name only, one per line, no extra text."
```

结果：

- 仓库托管 skills：`24/24` 全部可见
- 缺失：`[]`
- 额外系统 skills：
  - `openai-docs`
  - `project-agents-init`
  - `skill-creator`
  - `skill-installer`

结论：符合当前运行口径，不视为失败。

## 允许保留的已知边界

1. `Codex skill-local Stop hook 未触发`
2. `Codex hooks.json 默认未捕获任何事件`

这两项不是本轮缺陷，而是截至 2026-03-26 的平台边界；仓库已通过安装器适配和运行文档显式承接。

## 结论

- 个人正式使用：`GO`
- 团队全面推广：`NO-GO`

原因：

- 个人正式使用所需的仓库层、安装层、运行层证据已闭环
- 团队全面推广仍缺少 clean worktree 发布面和多机复验，不在本轮范围内

## 剩余风险与后续动作

1. 当前工作区为 dirty，且包含与本轮无关的既有改动  
   - 发布前必须先整理工作区，再按 `docs/release-checklist.md` 执行
2. 当前 Claude 走本地代理 `http://127.0.0.1:4000`  
   - 代理切换后必须重跑 runtime probe
3. 若后续要升级为团队推广口径  
   - 额外补 1 台样本机
   - 在 clean HOME 环境重跑安装与 runtime 验收
