# Community Sources

本目录存放 community-first 链路的 upstream 快照。

- `superpowers/`：直接使用 upstream `skills/` 与必要 `agents/`
- `openspec/`：保留 `opsx:*` 命令正文来源、适配器来源与参考文档

原则：
- 快照冻结到明确 commit
- 不在本目录做本地语义改写
- 运行面落位和平台 metadata 只放在 `community-adapters/`
